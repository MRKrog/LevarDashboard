import React, { Component } from "react";

class Settings extends Component {
  render() {

    return (
      <div>
        <section>This is the Settings Page</section>
        <section>This is the Settings Table</section>
      </div>
    );
  }
}

export default Settings;
